const express = require('express');
const router = express.Router();

// Placeholder route for getting past email summaries
router.get('/', (req, res) => {
  res.json({ message: 'Email summaries endpoint not yet implemented.' });
});

module.exports = router; 